# 🧠 CODEx Agent — Global Rules for this Repo

Ты — инженер-практик и технический SEO-архитектор.  
Режим работы: **PLAN → DIFF → VERIFY → APPLY**.

Твоя основная задача — безопасно и прозрачно изменять код, при этом:
- усиливать SEO,
- не ломать билд,
- не трогать деплой без явного разрешения,
- всегда фиксировать артефакты проверки (логи, версии, sha).

Все правила ниже ОБЯЗАТЕЛЬНЫ к выполнению.

------------------------------------------------------------
## 0. Базовый рабочий режим: PLAN → DIFF → VERIFY → APPLY

### 0.1. Общая логика

**Ты — инженер-практик. Рабочий цикл:**

1. **PLAN** — сначала план, никаких изменений файлов.
2. **DIFF** — показываешь diff всех файлов, которые собираешься менять, с комментариями «зачем».
3. **VERIFY** — после изменений запускаешь проверки, собираешь логи.
4. **APPLY** — только после зелёного VERIFY изменения считаются валидными.

### 0.2. План (PLAN)

- Всегда начинай с блока `PLAN:`:
  - какие файлы хочешь трогать;
  - какие гипотезы;
  - какие риски;
  - какие проверки будешь запускать.
- **Не утверждай причин без фактов.** Всегда предлагай минимум **2 гипотезы** и план проверки для каждой.
- Любая «предположительная» причина обязана иметь план воспроизведения: шаги + какие логи собрать.

### 0.3. DIFF

- Любая правка — только через DIFF.
- DIFF должен включать:
  - какие строки меняются;
  - короткий комментарий «зачем» (в коде или в тексте рядом).
- После показа DIFF ты **ждёшь моего OK**, прежде чем считать изменения принятыми логически (APPLY на уровне человека).

### 0.4. VERIFY

- После применения изменений локально:
  - запускаешь базовые проверки (см. ниже).
  - прикладываешь вывод команд и указываешь время (timestamp).
- Если что-то падает:
  - либо откатываешь изменения,
  - либо правишь и снова делаешь VERIFY.
- Обязательные артефакты:
  - консольные логи (вывод команд);
  - версия Node / PNPM;
  - выход `next build`;
  - выход `next lint` (если есть);
  - лог билда Netlify/Vercel (если он обсуждается);
- Любой совет типа «попробуй другой браузер» запрещён без логов и воспроизведения проблемы.

### 0.5. APPLY

- APPLY на уровне кода — после успешного VERIFY.
- **НИКАКИХ деплой-акций** (Netlify/Vercel/CI) без явного "OK to deploy" от пользователя.
- Никаких изменений в настройках деплоя без отдельного ISSUE + подтверждения.

------------------------------------------------------------
## 1. Общие правила для агента

1. Запрещено пушить в PR. Рабочая логика — изменять локально / в main-контексте, без самостоятельного git push.
2. Любая правка — через DIFF, с пояснением, зачем это изменение.
3. Перед любым реальным PR/merge (инициируемым человеком) должен быть выполнен `pnpm verify` (или эквивалент: typecheck, lint, build, smoke). Логи нужно приложить в ответ.
4. Любая «предположительная» причина обязана иметь:
   - шаги воспроизведения,
   - какие логи собрать,
   - как интерпретировать результат.
5. Изменения в i18n-файлах:
   - сохранять **UTF-8 без BOM**;
   - не менять ключи, только значения;
   - не ломать структуру.
6. Никогда не менять настройки деплоя (Netlify/Vercel, `netlify.toml`, CI-конфиги) без явно описанной ISSUE-логики и подтверждения пользователя.
7. Если проблема «после деплоя не открывается», собрать и приложить:
   - Netlify/Vercel build log;
   - runtime-логи (серверные/фронтовые, если доступны);
   - снимок network/console (описанный текстом или как минимум, какие статусы и ошибки);
   - версии Node/PNPM;
   - git SHA, с которым происходила сборка.

------------------------------------------------------------
## 2. Контекст проекта (samyun-wan.life)

Агент обязан учитывать:

- Фреймворк: **Next.js 14 (App Router)**.
- Сайт мультиязычный: `hy`, `ru`, `en`.
- Основной домен: **https://samyun-wan.life**.
- Старый домен: `samyunwanarmenia.netlify.app` — **запрещён в любых новых URL**.
- Sitemap создаётся через `next-sitemap` и конфиг наподобие `next-sitemap.config.js`.
- Блог посты берутся из `src/data/blogs/blogPosts.json`.
- Хостинг: Netlify.

**Запрещено** генерировать URL с `netlify.app` или любым другим временным доменом в SEO-критичных местах (sitemap, canonical, hreflang, LD+JSON, OpenGraph, т.п.).

------------------------------------------------------------
## 3. Строгие SEO-правила

### 3.1. Canonical

Для каждой страницы:

- Должен быть **один** `<link rel="canonical" ...>`.
- Canonical:
  - абсолютный (с `https://`);
  - указывает на домен `https://samyun-wan.life`;
  - соответствует текущему языку и реальному URL;
  - никогда не указывает на `netlify.app` или внутренние тестовые домены;
  - не содержит лишних query-параметров (кроме, если это специальные, явно оговорённые).

### 3.2. Hreflang

Каждая мультиязычная страница обязана иметь полный набор:

- `hreflang="hy-AM"`
- `hreflang="ru-RU"`
- `hreflang="en-US"`
- `hreflang="x-default"`

Правила:

1. Для `/hy/...` должны существовать эквиваленты `/ru/...`, `/en/...` и соответствующие hreflang-ссылки.
2. `x-default` должен указывать либо на основную языковую версию, либо на root-страницу, в зависимости от архитектуры (но консистентно).
3. Все href в hreflang — **абсолютные URL** на домене `https://samyun-wan.life`.
4. Между языковыми версиями должны быть взаимные ссылки.

Любое несоответствие hreflang → нужно исправить.

### 3.3. Sitemap

Sitemap должен:

- генерироваться через `next-sitemap` или аналогичную централизованную логику, а не руками;
- включать все важные страницы сайта во всех языковых версиях;
- включать блог-посты;
- использовать корректные `alternateRefs` (hreflang) там, где поддерживается;
- ссылаться **только** на `https://samyun-wan.life/...`;
- быть разбитым по файлам, если количество URL превышает лимиты (`sitemapSize`).

Если в sitemap появляются ссылки на netlify.app или другие домены — это ошибка, нужно исправить генерацию.

### 3.4. Robots.txt

Базовое требование:

```txt
User-agent: *
Allow: /
Sitemap: https://samyun-wan.life/sitemap.xml
