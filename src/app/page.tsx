import type { Metadata } from 'next';

export const metadata: Metadata = {
  robots: {
    index: false,
    follow: false,
  },
};

const RootPage = () => {
  return null;
};

export default RootPage;
