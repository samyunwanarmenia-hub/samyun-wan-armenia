"use client";

import BenefitsSection from '@/components/BenefitsSection';

// Removed BenefitsPageProps as _params is no longer used
const BenefitsPage = () => { // Removed { params: _params }
  return (
    <BenefitsSection />
  );
};

export default BenefitsPage;